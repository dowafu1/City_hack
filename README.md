# City_hack

# **ЦМП Томской области – Telegram-бот** 🚀

Телеграм-бот для психологической помощи, информирования и связи с Центром молодежной политики Томской области. 🌟

## 📋 Содержание

- [Возможности](#-возможности)
- [Навигация в боте](#-навигация-в-боте)
- [Админ-панель](#-админ-панель)
- [AI-интеграция](#-ai-интеграция)
- [Структура проекта](#-структура-проекта)
- [Технологии](#-технологии)
- [Установка и запуск](#-установка-и-запуск)
- [Переменные окружения](#-переменные-окружения)
- [Структура БД](#-структура-бд)
- [Логи и троттлинг](#-логи-и-троттлинг)
- [Тестирование](#-тестирование)
- [Команда](#-команда)

---

## 🚀 Возможности

### 1. Куда обращаться (контакты) 📞

- Отображает список контактов служб: категория, название, телефон, описание.
- Источник: таблица `contacts`.
- ⚠️ Пока нет кнопок "Позвонить/Скопировать/Перейти на сайт" — информация выводится текстом.

### 2. Тревожная кнопка (SOS) 🆘

- Показывает инструкцию из таблицы `sos_instructions`. Если пусто — дефолтный текст: «При опасности звоните 112 или 102…».

### 3. Навигатор помощи 🧭

- Разделы: «Мне нужна помощь» 😟, «Хочу сообщить о проблеме» 🚨, «Просто хочу поговорить» 🧠.
- Контент берётся из `articles` по категориям: `help_me_<role>`, `report_<role>`, `other_<role>`, где `<role>` — `teen` или `parent`.

### 4. Анонсы мероприятий 📅

- Выводит список событий из `events`: название, дата, описание, ссылка.
- Уведомления о событиях не реализованы пока.

### 5. Совет дня / Подписка 💡

- Кнопка «Подписаться на поддержку» добавляет пользователя в `subs` и планирует отправку случайного совета раз в день.
- Отправка происходит фоновым таском `notifier()`.
- Повторное нажатие — отмена подписки.
- Советы берутся из `tips`. Если пусто — дефолтный совет.
- Советы могут генерироваться с помощью AI.

### 6. Обратная связь (вопрос) ❓

- По кнопке «Задать вопрос» бот просит ввести текст.
- Вопрос сохраняется в `questions` с `user_id` и timestamp.
- Пересылка экспертам и ответы пользователю не реализованы пока, но история чата логируется для потенциальной AI-обработки.

### 7. Опросы 📊

- Раздел есть, но сейчас показывает «Пока опросов нет». Работа с таблицей `polls` не реализована.

### 8. Роли пользователей 👥

- При `/start` пользователь выбирает: «Я подросток» или «Я родитель». Роль влияет на контент в навигаторе.

### 9. Хранение истории чата 📝

- Все взаимодействия логируются в таблицу `chat_history` для анализа или будущей AI-обработки.

---

## 🧭 Навигация в боте

- `/start` — приветствие + выбор роли. Далее — главное меню:

  - 🆘 Тревожная кнопка
  - 🧭 Мне срочно нужна помощь
  - 📞 Куда обратиться?
  - ❓ Задать вопрос
  - 📅 Мероприятия
  - 💡 Получить совет
  - 🔔 Подписаться на поддержку
  - 🔄 Изменить роль
  - 🛠️ Админ-панель

---

## 🛡️ Админ-панель

Доступна пользователям с ID в `ADMIN_IDS`. Форматы ввода:

### Контакты (`ad_contacts`) 📒

- Формат: `category|name|+7(XXX)XXX-XX-XX|description`
- Телефон валидируется по шаблону `+7(XXX)XXX-XX-XX`.

### SOS (`ad_sos`) 🆘

- Ввод: любой текст. Перезаписывает единственную инструкцию.

### Событие (`ad_event`) 📅

- Формат: `title|YYYY-MM-DD|description|link`

### Статья (`ad_article`) 📝

- Формат: `category|title|content`
- Рекомендуемые категории: `help_me_teen`, `help_me_parent`, `report_teen`, `report_parent`, `other_teen`, `other_parent`.

### Совет (`ad_tip`) 💡

- Ввод: любой текст.

Все операции подтверждаются сообщением «✅ Сохранено» или ошибкой «❌ Неверный формат».

---

## 🤖 AI-интеграция

- **Цепочка AI (ai_chain.py):** Использует Sber AI (GigaChat) и Mistral AI для генерации/улучшения ответов.
  - `chainize()`: Генерирует ответ на запрос пользователя через Sber, затем улучшает его с помощью Mistral.
  - `get_tip()`: Генерирует новый "совет дня" на основе предыдущих, используя Sber AI.
- **Использование:** Пока не интегрировано напрямую в бота. Может использоваться для генерации контента, который потом добавляется в БД админами.
- **Библиотеки:** langchain-gigachat, mistralai.
- **Переменные:** `SBER_TOKEN`, `MISTRAL_TOKEN` в .env.
- **Дополнительные возможности:** Можно загружать файлы из папки `context` для анализа и интеграции в AI-цепочку. Также поддерживается добавление других нейросетей для улучшения качества ответов.

---

# 🏗️ Структура проекта

Проект организован в модульную структуру для удобства разработки, тестирования и масштабирования:

- **ai/** 📁 — Модуль для AI-интеграций и цепочек обработки.
  - `__init__.py` — Инициализация модуля.
  - `ai_chain.py` — Основная цепочка AI (функции `chainize` и `get_tip` для генерации и улучшения ответов).
  - `mistral_ai.py` — Интеграция с Mistral AI.
  - `sber_ai.py` — Интеграция с Sber AI (GigaChat).
  - **context/** 📁 — Папка для хранения файлов с дополнительным контекстом для AI-цепочек (например, промпты, данные для анализа).

- **backend/** 📁 — Основной backend бота и базы данных.
  - `__init__.py` — Инициализация модуля.
  - `bot.py` — Логика Telegram-бота (aiogram, FSM, обработчики сообщений и колбэков).
  - `db.py` — Работа с PostgreSQL (asyncpg, функции для БД: init_db, CRUD-операции).

- **tests/** 📁 — Модуль для unit-тестов.
  - `test.py` — Общие тесты для бота и AI-компонентов.
  - `test_DB.py` — Тесты для базы данных (проверки соединения, запросов и т.д.).

- **Другие файлы в корне:**
  - `README.md` 📄 — Документация проекта.
  - `requirements.txt` 📋 — Список зависимостей.
  - `.env` 🔑 — Файл с переменными окружения (не коммитится).

Эта структура обеспечивает четкое разделение логики: AI, бот и тесты изолированы, что упрощает разработку, отладку и масштабирование. 🛠️

---

## 🧪 Тестирование

### Тесты в проекте

Unit-тесты с использованием pytest для ключевых компонентов:

- **Middleware:**
  - Троттлинг: проверка ограничения запросов, обработка превышения лимита.

- **БД-операции:**
  - init_db: создание таблиц.
  - get/set_role: чтение/запись ролей.
  - log: логирование действий.
  - CRUD-операции для articles, contacts, events, tips и других таблиц.

- **Обработчики сообщений и колбэков:**
  - start: приветствие и выбор роли.
  - roles: смена роли.
  - navigation: переходы по меню.
  - admin panel: добавление/редактирование контента.
  - subscription: подписка/отписка.
  - question saving: сохранение вопросов.
  - AI-интеграции: chainize и get_tip.

### Дополнительные тесты

- Проверки на валидацию входных данных: формат телефона в контактах, дата в событиях.
- Обработку ошибок: пустая БД, неверные форматы ввода.
- Интеграционные тесты для FSM: состояния в админ-панели.

### Результаты

- Все тесты прошли успешно (100% pass rate).
- Покрытие кода: 87% (измерено с помощью coverage.py).
- Основные функции полностью проверены: бот-логика, FSM, DB-интеракции, AI-цепочки.
- Рекомендации по улучшению: добавить интеграционные тесты для полного цикла и e2e-тесты для бота в реальном окружении.

---

## 🛠️ Технологии

- **Язык:** Python 3.11+ 🐍
- **Фреймворк:** aiogram 3.x 🤖
- **Хранилище:** PostgreSQL (asyncpg) 🗄️
- **FSM:** aiogram.fsm + MemoryStorage 🔄
- **AI:** langchain-gigachat (Sber AI), mistralai (Mistral AI) 🧠
- **Другое:** python-dotenv, asyncpg, pytest 📦

---

## 📦 Установка и запуск

### Требования

- Python 3.11+
- Git
- PostgreSQL

### Клонирование

```bash
git clone https://github.com/dowafu1/City_hack.git
cd City_hack
```

### Виртуальное окружение и зависимости

```bash
python -m venv .venv
source .venv/bin/activate 
pip install --upgrade pip
pip install -r requirements.txt
```

> Файл `requirements.txt` содержит все необходимые зависимости: aiogram, python-dotenv, asyncpg, langchain-gigachat, mistralai, pytest.

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=123456789:AA...
ADMIN_IDS=123456789,987654321
SBER_TOKEN=your_sber_token
MISTRAL_TOKEN=your_mistral_token
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASS=your_db_pass
DB_NAME=cmp_bot
```

### Запуск

```bash
python bot.py
```

> Таблицы в БД создаются автоматически при первом запуске. База данных должна быть создана заранее.

---

## 🔧 Переменные окружения

- `BOT_TOKEN` — обязательна
- `ADMIN_IDS` — необязательна
- `SBER_TOKEN` — для Sber AI
- `MISTRAL_TOKEN` — для Mistral AI
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASS`, `DB_NAME` — для PostgreSQL (дефолты: localhost:5432, user=текущий логин, pass="", db=cmp_bot)

---

## 🗄️ Структура БД

Таблицы создаются в `init_db()`:

- `users(user_id PK, role)`
- `articles(id PK, category, title, content)`
- `contacts(id PK, category, name, phone, description)`
- `sos_instructions(id PK, text)`
- `events(id PK, title, date, description, link)`
- `questions(id PK, user_id, question, timestamp)`
- `tips(id PK, text)`
- `polls(id PK, poll_id, results)`
- `logs(id PK, user_id, action, timestamp)`
- `subs(user_id PK, next_at)`
- `chat_history(id PK, chat_id, role, content, timestamp)`

---

## 📈 Логи и троттлинг

- **Логи:** Некоторые действия пишутся в `logs`: start, переходы по разделам, tip и пр.
- **История чата:** В `chat_history` логируются сообщения пользователя и бота: роли 'user'/'ai'.
- **Троттлинг:** Middleware ограничивает частоту для одного пользователя: ~10 запросов/сек.

---

## 👥 Команда

- Даниил Татарников — Тимлид, Backend-разработчик
- Шевченко Егор — ML-инженер, Backend-разработчик, Тестировщик
- Щилко Максим — ML-инженер, Backend-разработчик
- Дарья Климова — Frontend-разработчик, Аналитик, Дизайнер