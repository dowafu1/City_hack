# City_hack

# **ЦМП Томской области – Telegram-бот**

Телеграм-бот для психологической помощи, информирования и связи с Центром молодежной политики Томской области.


## 📋 Содержание

* [Возможности](#-возможности)
* [Навигация в боте](#-навигация-в-боте)
* [Админ-панель](#-админ-панель)
* [AI-интеграция](#-ai-интеграция)
* [Технологии](#-технологии)
* [Установка и запуск](#-установка-и-запуск)
* [Переменные окружения](#-переменные-окружения)
* [Структура БД](#-структура-бд)
* [Логи и троттлинг](#-логи-и-троттлинг)

---

## 🚀 Возможности

#### 1) Куда обращаться (контакты)

* Отображает список контактов служб: категория, название, телефон, описание.
* Источник: таблица `contacts`.
* ⚠️ Сейчас **нет кнопок "Позвонить/Скопировать/Перейти на сайт"** — выводится текстом.

#### 2) Тревожная кнопка (SOS)

* Показ инструкции из таблицы `sos_instructions` (берётся 1 запись). Если пусто — дефолтный текст: «При опасности звоните 112 или 102…»

#### 3) Навигатор помощи

* Разделы: «Мне нужна помощь», «Хочу сообщить о…», «Просто хочу поговорить».
* Контент берётся из `articles` по категориям: `help_me_<role>`, `report_<role>`, `other_<role>`, где `<role>` — `teen`/`parent`.

#### 4) Анонсы мероприятий

* Выводит список событий из `events` (название, дата, описание, ссылка).
* Уведомления о событиях **не реализованы**.

#### 5) Совет дня / Подписка

* Кнопка «Подписаться на поддержку» добавляет юзера в `subs` и запланирует отправку случайного совета раз в день.
* Отправка происходит фоновым таском `notifier()`.
* Повторное нажатие — отмена подписки.
* Советы берутся из `tips` (случайная запись). Если пусто — дефолтный совет.
* Советы могут генерироваться с помощью AI (см. [AI-интеграция](#-ai-интеграция)).

#### 6) Обратная связь (вопрос)

* По кнопке «Задать вопрос» бот просит ввести текст.
* Вопрос сохраняется в `questions` с `user_id` и timestamp.
* Пересылка экспертам и ответы пользователю **пока не реализованы** (но история чата логируется для потенциальной AI-обработки).

#### 7) Опросы

* Раздел есть, но сейчас показывает «Пока опросов нету 📊». Работа с таблицей `polls` не реализована.

#### 8) Роли пользователей

* При `/start` пользователь выбирает: «Я подросток» или «Я родитель». Роль влияет на контент в навигаторе.

#### 9) Хранение истории чата

* Все взаимодействия (вопросы пользователя, ответы бота) логируются в таблицу `chat_history` для анализа или будущей AI-обработки.

---

## 🧭 Навигация в боте

* `/start` — приветствие + выбор роли (если не задана). Далее — **главное меню**:

  * 🆘 Тревожная кнопка
  * 🧭 Мне срочно нужна помощь
  * 📞 Куда обратиться?
  * ❓ Задать вопрос
  * 📅 Мероприятия
  * 💡 Получить совет
  * 🔔 Подписаться на поддержку (тоггл)
  * 🔄 Изменить роль
  * 🛠️ Админ-панель (видна только админам из `ADMIN_IDS`)

---

## 🛡️ Админ-панель

Доступна пользователям с ID в `ADMIN_IDS`. Форматы ввода:

#### Контакты (`ad_contacts`)

* Формат: `category|name|+7(XXX)XXX-XX-XX|description`
* Телефон валидируется по шаблону `+7(XXX)XXX-XX-XX`.

#### SOS (`ad_sos`)

* Ввод: любой текст. Перезаписывает единственную инструкцию.

#### Событие (`ad_event`)

* Формат: `title|YYYY-MM-DD|description|link`

#### Статья (`ad_article`)

* Формат: `category|title|content`
* Рекомендуемые категории: `help_me_teen`, `help_me_parent`, `report_teen`, `report_parent`, `other_teen`, `other_parent`.

#### Совет (`ad_tip`)

* Ввод: любой текст (добавляет в пул советов).

Все операции подтверждаются сообщением «Сохранено» или ошибкой «Неверный формат».

---

## 🤖 AI-интеграция

* **Цепочка AI (ai_chain.py):** Использует Sber AI (GigaChat) и Mistral AI для генерации/улучшения ответов.
  * `chainize()`: Генерирует ответ на запрос пользователя через Sber, затем улучшает его с помощью Mistral (как эмпатичный психолог).
  * `get_tip()`: Генерирует новый "совет дня" на основе предыдущих, используя Sber AI.
* **Использование:** Пока не интегрировано напрямую в бота (тестируется в `main()`). Может использоваться для генерации контента (советов, статей), который потом добавляется в БД админами.
* **Библиотеки:** langchain-gigachat (для Sber), mistralai.
* **Переменные:** `SBER_TOKEN`, `MISTRAL_TOKEN` в .env.
* **Дополнительные возможности:** Можно загружать файлы из папки `context` для анализа и интеграции в AI-цепочку (например, для предоставления дополнительного контекста в промптах). Также поддерживается добавление других нейросетей (например, через интеграцию с API OpenAI, Anthropic или аналогичными) для дальнейшего улучшения качества ответов, путем расширения функции `chainize()` или создания новых цепочек.

---

## 🛠 Технологии

* **Язык:** Python 3.11+
* **Фреймворк:** aiogram 3.x
* **Хранилище:** **PostgreSQL** (asyncpg; база создаётся вручную, таблицы — автоматически в `init_db()`)
* **FSM:** `aiogram.fsm` + `MemoryStorage`
* **AI:** langchain-gigachat (Sber AI), mistralai (Mistral AI)
* **Другое:** python-dotenv, asyncpg
* **Хостинг:** любой (VPS, bare metal, PaaS); Docker в коде не используется

---

## 📦 Установка и запуск

#### Требования

* Python 3.11+
* Git
* PostgreSQL (установите сервер, создайте БД и пользователя)

#### Клонирование

```bash
git clone https://github.com/dowafu1/City_hack.git
cd City_hack
```

#### Виртуальное окружение и зависимости

```bash
python -m venv .venv
source .venv/bin/activate 
pip install --upgrade pip
pip install aiogram python-dotenv asyncpg langchain-gigachat mistralai
```

#### Переменные окружения

Создайте файл `.env` в корне:

```env
BOT_TOKEN=123456789:AA...    # токен бота от @BotFather
ADMIN_IDS=123456789,987654321 # список ID админов через запятую
SBER_TOKEN=your_sber_token    # токен для Sber AI (GigaChat)
MISTRAL_TOKEN=your_mistral_token # токен для Mistral AI
DB_HOST=localhost             # хост PostgreSQL
DB_PORT=5432                  # порт PostgreSQL
DB_USER=your_db_user          # пользователь БД
DB_PASS=your_db_pass          # пароль БД
DB_NAME=cmp_bot               # имя БД (создайте заранее)
```

#### Запуск

```bash
python bot.py
```

> Таблицы в БД создаются автоматически при первом запуске. База данных должна быть создана заранее.

---

## 🔧 Переменные окружения

* `BOT_TOKEN` — обязательна
* `ADMIN_IDS` — необязательна
* `SBER_TOKEN` — для Sber AI
* `MISTRAL_TOKEN` — для Mistral AI
* `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASS`, `DB_NAME` — для PostgreSQL (дефолты: localhost:5432, user=текущий логин, pass="", db=cmp_bot)

---

## 🗄️ Структура БД

Таблицы создаются в `init_db()`:

* `users(user_id PK, role)`
* `articles(id PK, category, title, content)`
* `contacts(id PK, category, name, phone, description)`
* `sos_instructions(id PK, text)`
* `events(id PK, title, date, description, link)`
* `questions(id PK, user_id, question, timestamp)`
* `tips(id PK, text)`
* `polls(id PK, poll_id, results)` *(зарезервировано)*
* `logs(id PK, user_id, action, timestamp)`
* `subs(user_id PK, next_at)`
* `chat_history(id PK, chat_id, role, content, timestamp)` — история сообщений (user/ai)

---

## 📈 Логи и троттлинг

* **Логи:** некоторые действия пишутся в `logs` (`start`, переходы по разделам, tip и пр.).
* **История чата:** В `chat_history` логируются сообщения пользователя и бота (роли: 'user'/'ai').
* **Троттлинг:** middleware ограничивает частоту для одного пользователя (по умолчанию \~10 запросов/сек.)

---

## 👥 Команда

* Даниил Татарников — Тимлид, Backend-разработчик
* Шевченко Егор — ML-инженер, Backend-разработчик, Тестировщик
* Щилко Максим — ML-инженер, Backend-разработчик
* Дарья Климова — Frontend-разработчик, Аналитик, Дизайнер