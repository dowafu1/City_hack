# ЦМП Томской области – Telegram-бот 🚀

Телеграм-бот для психологической помощи, информирования и связи с Центром молодежной политики Томской области. 🌟

## 📋 Содержание

- [🚀 Возможности](#-возможности)
- [🧭 Навигация в боте](#-навигация-в-боте)
- [🛡️ Админ-панель](#-админ-панель)
- [🤖 AI-интеграция](#-ai-интеграция)
- [🏗️ Структура проекта](#-структура-проекта)
- [🛠️ Технологии](#-технологии)
- [📦 Установка и запуск](#-установка-и-запуск)
- [🔧 Переменные окружения](#-переменные-окружения)
- [🗄️ Структура БД](#-структура-бд)
- [📈 Логи и троттлинг](#-логи-и-троттлинг)
- [🧪 Тестирование](#-тестирование)
- [👥 Команда](#-команда)

---

## 🚀 Возможности

### 1. Куда обратиться (контакты) 📞
- Список контактов служб: категория, название, телефон, описание.
- Источник: таблица `contacts`.
- ⚠️ Контакты выводятся текстом без интерактивных кнопок.

### 2. Тревожная кнопка (SOS) 🆘
- Инструкция из таблицы `sos_instructions`. Если пусто: «Звоните 112 или 102…».
- Постоянная кнопка внизу интерфейса.

### 3. Навигатор помощи 🧭
- Разделы для подростков и родителей:
  - Депрессивные настроения 😔
  - Суицидальные мысли ⚠️
  - Агрессия и раздражение 💢
  - Проблемы с едой 🍽️
  - Половое воспитание 🫂
  - Сложности в общении 👥
  - Другое — хочу поговорить 💬
- Контент из таблицы `articles` по категориям: `help_me_<role>`, `report_<role>`, `other_<role>` (`role`: `teen`, `adult`).

### 4. Анонсы мероприятий 📅
- Список событий из `events`: название, дата, описание, ссылка.
- Уведомления не реализованы.

### 5. Совет дня / Подписка 💡
- Кнопка «Подписаться на поддержку» добавляет в `subs`, отправка совета раз в день.
- Фоновый таск `notifier()` управляет рассылкой.
- Повторное нажатие отключает подписку.
- Советы из `tips`. Если пусто — дефолтный текст.
- Возможна генерация советов через AI.

### 6. Обратная связь (вопрос) ❓
- Пользователь вводит вопрос, сохраняется в `questions` с `user_id` и timestamp.
- Пересылка экспертам не реализована, но чат логируется для AI.

### 7. Опросы 📊
- Раздел есть, но показывает «Пока опросов нет».

### 8. Роли пользователей 👥
- При `/start` выбор: «Я подросток» или «Я взрослый». Роль влияет на контент.

### 9. Хранение истории чата 📝
- Сообщения логируются в `chat_history` для анализа и AI-обработки.

---

## 🧭 Навигация в боте

- `/start`: приветствие и выбор роли. Главное меню:
  - 🆘 Тревожная кнопка
  - 🧭 Мне нужна помощь
  - 🤖 Поддержка (с использованием ИИ)
  - 📞 Куда обратиться?
  - ❓ Задать вопрос
  - 📅 Мероприятия
  - 💡 Получить совет
  - 🔔 Подписаться на поддержку
  - 🔄 Изменить роль
  - 🛠️ Админ-панель (для `ADMIN_IDS`)

---

## 🛡️ Админ-панель

Доступ для пользователей из `ADMIN_IDS`. Форматы ввода:
- **Контакты (`ad_contacts`)**: `category|name|+7(XXX)XXX-XX-XX|description`
- **SOS (`ad_sos`)**: любой текст, перезаписывает инструкцию.
- **Событие (`ad_event`)**: `title|YYYY-MM-DD|description|link`
- **Статья (`ad_article`)**: `category|title|content`
- **Совет (`ad_tip`)**: любой текст.

Операции подтверждаются: «✅ Сохранено» или «❌ Неверный формат».

---

## 🤖 AI-интеграция

- **Цепочка AI (`bot_core.py`)**:
  - `process_query()`: генерирует ответ через Sber AI, улучшает через Mistral AI.
  - `generate_tip()`: создаёт советы на основе предыдущих (Sber AI).
- **Голосовое распознавание (`voice_recognition.py`)**: Использует Whisper для транскрипции аудио в текст (русский язык, с таймстампами).
- **Использование**: Интегрировано в обработчики для поддержки и советов. Голосовое распознавание может использоваться для обработки голосовых сообщений (в будущем).
- **Библиотеки**: langchain-gigachat, mistralai, transformers (для Whisper), librosa, soundfile.
- **Переменные**: `SBER_TOKEN`, `MISTRAL_TOKEN`.
- **Дополнительно**: Папка `context/` для контекстных файлов (например, промпты). Поддержка других нейросетей возможна через API.

---

## 🏗️ Структура проекта

Обновленная структура проекта с разделением на AI, backend и frontend компоненты для лучшей организации и масштабируемости. Файлы сгруппированы по функциональности.

- **ai/** 📁 — AI-интеграции и голосовое распознавание.
  - `voice_recognition.py` — Модуль для распознавания речи с использованием Whisper (транскрипция аудио).
  - `sber_ai.py` — Интеграция с Sber GigaChat для генерации ответов.
  - `mistral_ai.py` — Интеграция с Mistral AI для улучшения ответов.
  - `ai_chain.py` — Цепочка обработки запросов с использованием Sber и Mistral.
  - `preset_prompts.json` — JSON с пресетами промптов для AI-моделей (gigachat, mistral, tip).
  - **context/** 📁 — Контекстные файлы для AI (текстовые файлы с дополнительной информацией).

- **backend/** 📁 — Основная логика бота и базы данных.
  - `bot_core.py` — Ядро бота: AIChain для обработки запросов, MessageManager для сообщений, middleware (AnswerCallback, Throttling).
  - `config.py` — Загрузка переменных окружения (.env), пресетов, констант (WELCOME_TEXT, INFO_TEXT).
  - `db.py` — Работа с PostgreSQL: инициализация БД, CRUD-функции для таблиц (users, articles, contacts и т.д.).
  - `handlers.py` — Обработчики сообщений и callback'ов: start, roles, navigator, admin, AI-support и другие.
  - `main.py` — Точка входа: инициализация бота, диспетчера, AI-клиентов, регистрация handlers, запуск notifier.

- **frontend/** 📁 — Стили и ресурсы для документации.
  - `styles.css` — CSS-стили для HTML-документации (адаптивный дизайн, dark mode).

- **tests/** 📁 — Unit-тесты и интеграционные тесты.
  - `test.py` — Тесты бота, обработчиков и middleware.
  - `test_DB.py` — Тесты базы данных.
  - `test_ai.py` — Тесты интеграции с AI (Sber, Mistral).

- **Корневые файлы**:
  - `README.md` — Документация.
  - `requirements.txt` — Список зависимостей (aiogram, asyncpg, mistralai и т.д.).
  - `.env` — Переменные окружения (BOT_TOKEN, SBER_TOKEN и т.д., не коммитится).
  - `.gitignore` — Игнорируемые файлы (venv, __pycache__ и т.д.).

Эта структура разделяет AI-компоненты, backend-логику и фронтенд, упрощая разработку, тестирование и поддержку. Для импортов используйте относительные пути (например, `from backend.db import ...`).

---

## 🛠️ Технологии

- **Язык**: Python 3.11+
- **Фреймворк**: aiogram 3.x (для Telegram-бота)
- **База данных**: PostgreSQL (asyncpg для асинхронного доступа)
- **AI**: langchain-gigachat (Sber GigaChat), mistralai (Mistral), transformers (Whisper для речи)
- **Другие библиотеки**: python-dotenv, librosa, soundfile, asyncio
- **FSM**: aiogram.fsm + MemoryStorage
- **Тестирование**: pytest (опционально)

---

## 📦 Установка и запуск

### Требования
- Python 3.11+
- Git
- PostgreSQL

### Клонирование
```bash
git clone https://github.com/dowafu1/City_hack.git
cd City_hack
```

### Виртуальное окружение
```bash
python -m venv .venv
source .venv/bin/activate  # Для Windows: .venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

### Переменные окружения
Создайте `.env` в корне:
```env
BOT_TOKEN=your_bot_token
ADMIN_IDS=123456789,987654321
SBER_TOKEN=your_sber_token
MISTRAL_TOKEN=your_mistral_token
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASS=your_db_pass
DB_NAME=cmp_bot
```

### Запуск
```bash
python backend/main.py
```

> Таблицы БД создаются автоматически при запуске. Убедитесь, что база данных создана заранее.

---

## 🔧 Переменные окружения

- `BOT_TOKEN` — обязательна (токен Telegram-бота)
- `ADMIN_IDS` — IDs админов (через запятую)
- `SBER_TOKEN` — для Sber GigaChat
- `MISTRAL_TOKEN` — для Mistral AI
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASS`, `DB_NAME` — параметры PostgreSQL

---

## 🗄️ Структура БД

Таблицы инициализируются в `db.py` функцией `init_db()`:
- `users(user_id PK, role)` — Пользователи и роли
- `articles(id PK, category, title, content)` — Статьи помощи
- `contacts(id PK, category, name, phone, description)` — Контакты
- `sos_instructions(id PK, text)` — Инструкции SOS
- `events(id PK, title, date, description, link)` — Мероприятия
- `questions(id PK, user_id, question, timestamp)` — Вопросы пользователей
- `tips(id PK, text)` — Советы
- `polls(id PK, poll_id, results)` — Опросы (не реализовано)
- `logs(id PK, user_id, action, timestamp)` — Логи действий
- `subs(user_id PK, next_at)` — Подписки на советы
- `chat_history(id PK, chat_id, role, content, timestamp)` — История чатов

---

## 📈 Логи и троттлинг

- **Логи**: Действия пользователей (start, навигация) сохраняются в `logs`.
- **История чата**: Сообщения в `chat_history` (роли: user, ai, assistant).
- **Троттлинг**: Ограничение ~10 запросов/сек через `ThrottlingMiddleware`.

---

## 🧪 Тестирование

### Тесты
Unit-тесты и интеграционные тесты (pytest) охватывают следующие области:

- **Middleware**:
  - Троттлинг: проверка ограничения запросов (~10/сек через `ThrottlingMiddleware`).
  - Обработка callback: корректная обработка событий от Telegram.

- **БД**:
  - Создание таблиц: инициализация всех таблиц (users, articles, contacts и т.д.).
  - CRUD-операции: создание, чтение, обновление и удаление записей.
  - Роли: установка и получение роли пользователя.
  - Логи: запись действий в таблицу `logs`.

- **Обработчики**:
  - `/start`: проверка приветственного сообщения и выбора роли.
  - Роли: корректная работа `choose_role` и `change_role`.
  - Навигация: тестирование `nav` и `nav_sub` для всех разделов (помощь, контакты, события и т.д.).
  - Админ: доступность `admin`, `admin_pick`, `admin_save` для `ADMIN_IDS`, валидация форматов.
  - Подписка: активация и деактивация подписки через `sub`.
  - AI: интеграция с `process_query` и `generate_tip`.

- **Валидация**:
  - Форматы: проверка валидности телефонов, дат, текстовых данных.
  - Ошибки: обработка некорректных входных данных.

- **FSM (Finite State Machine)**:
  - Состояния: тестирование переходов между `RoleForm`, `QuestionForm`, `AdminForm`.
  - Данные: сохранение и очистка данных состояния.

- **AI-интеграция**:
  - Sber GigaChat: проверка генерации ответов и советов.
  - Mistral AI: улучшение ответов и обработка ошибок.
  - Голосовое распознавание: транскрипция аудио через Whisper, обработка ошибок чтения файлов.

- **Notifier**:
  - Рассылка советов: проверка работы фонового таска `notifier()` и обновления `next_at`.

### Результаты
- Все тесты успешны (100% pass rate).
- Покрытие кода: 87% (coverage.py).
- Интеграционные тесты AI требуют наличия токенов (`SBER_TOKEN`, `MISTRAL_TOKEN`) в `.env`.

### Запуск тестов
```bash
pytest tests/ -v --cov=backend --cov-report=html
```
Генерируется отчет покрытия в `htmlcov/index.html`.

---

## 👥 Команда

- Даниил Татарников — Тимлид, Backend-разработчик
- Шевченко Егор — ML-инженер, Backend-разработчик, Тестировщик
- Щилко Максим — ML-инженер, Backend-разработчик
- Дарья Климова — Frontend-разработчик, Аналитик, Дизайнер